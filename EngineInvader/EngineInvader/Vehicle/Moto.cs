namespace EngineInvader
{
    public class Moto : Player
    {
        public Moto(int x, int y) : base(x, y)
        {
        }
        //private float speed = 0.7f;
        //Moto offset;
        //// Start est appelé avant le premier update
        //void Start()
        //{

        //}
        //// Update est appelé une fois à chaque boucle
        //void Update()
        //{
        //    offset = new Moto(0, Time.time * speed);
        //}
    }
}
